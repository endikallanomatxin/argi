RealNumber : Abstract = [
	operator +(_, _) : Int
	operator -(_, _) : Int
	operator *(_, _) : Int
	operator /(_, _) : Int
	operator %(_, _) : Int
	operator ^(_, _) : Int
	...
]

RealNumber canbe [Int, Float]

-- Extended further in the math library

-- TODO: Pensar hasta que punto tiene sentido este abstract en core.


